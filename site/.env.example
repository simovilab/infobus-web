# Public URL, used for OG Image when running nuxt generate
NUXT_PUBLIC_SITE_URL=

# bUCR's real GTFS zip URL, downloaded+parsed server-side (hourly) to build
# the schedule shown on the site. Leave empty to use the default
# (feeds.simovi.org/bucr/schedule/gtfs.zip).
NUXT_GTFS_ZIP_URL=
