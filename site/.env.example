# Public URL, used for OG Image when running nuxt generate
NUXT_PUBLIC_SITE_URL=

# Static GTFS API base URL (bucr), fetched server-side to build the
# schedule shown on the site. Leave empty to use the default
# (raw.githubusercontent.com/simovilab/bucr/...).
NUXT_GTFS_API_BASE=
